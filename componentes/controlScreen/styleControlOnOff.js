import { StyleSheet } from "react-native"

const stylesControlOnOFF = StyleSheet.create({
    container:{
        marginTop:50,
        flexDirection:'row',
        justifyContent:'space-around'
    }
});

export default stylesControlOnOFF